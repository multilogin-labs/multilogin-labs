# Example 02 · Bulk create profiles from CSV

```bash
pip install -r ../../requirements.txt
export MULTILOGIN_TOKEN=...
export MULTILOGIN_FOLDER_ID=...
python bulk_create.py profiles.csv
```

See [cookbook 55 — CSV import](../../docs/api/cookbook/55-csv-import-profiles.md).
